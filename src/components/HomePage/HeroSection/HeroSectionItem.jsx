import { Box } from '@mui/material'
import React from 'react'

const HeroSectionItem = ({color}) => {
  return (
    <Box sx={{flex: "0 0 100%", bgcolor: color, height: "100vh"}}>HeroSectionItem</Box>
  )
}

export default HeroSectionItem